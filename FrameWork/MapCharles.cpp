#include "MapCharles.h"

MapCharles::MapCharles(const char * imgPath, const char * txtPath, const char * mapObjectPath, const char * waterTopPath, const char * waterBottomPath, const char * sewerPath) : GameMap(imgPath, txtPath, mapObjectPath)
{
	//GameMap::GameMap(imgPath, txtPath, mapObjectPath);
}